<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FormulaQ Solutions</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            background-color: #b0b0b0;
        }

        .container {
            text-align: left;
            background-color: #ffffff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        
        img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 10px;
        }
        .container2{
            display: flex;
            display: row;
            align-items: center;
            gap: 5px;
        }
        p {
            font-size: 18px;
            font-weight: 700;
            margin-bottom: 10px;
            color: #555;
        }

        a {
            color: #4285F4;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        
        <p><img src="<%= pic %>"></p>
        <div class="container2">
            <p>Hello <%= name %></p>
            <a href="/logout">[Sign Out]</a>
        </div>
        
        <p>You are signed in with email <%= email %></p>
        <p>Indian Time: <%= new Date().toLocaleString("en-US", { timeZone: "Asia/Kolkata" }) %></p>
        
    </div>
</body>
</html>

<!-- <html>
    <head>
        <title>FormulaQ Solutions</title>
    </head>
    <body>
        <h1>I am in home page</h1>
        <p><img src="<%= pic %>"></p>
        <p>Hello  <%= name %></p>
        <p>Email : <%= email %></p>
        <a href="/logout">Sign Out</a>
    </body>
</html> -->